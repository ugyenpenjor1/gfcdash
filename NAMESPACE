# Generated to match the roxygen2 tags in R/*.R.
# Regenerate authoritatively with devtools::document() once you have
# roxygen2 installed - this hand-built version is a correct starting point
# but devtools::document() should be treated as the source of truth going
# forward.
#
# CRITICAL: the import() order below is not arbitrary - it exactly matches
# the library() call order in the original single-file script:
#
#   shiny, shinydashboard, shinyjs, shinyWidgets, sf, terra, leaflet,
#   leafem, mapgl, DT, ggplot2, patchwork, dplyr, tidyr, tidyterra, scales,
#   animation, jsonlite
#
# When two packages export a function with the same name, the LAST one
# imported wins (identical mechanics to library() attachment order). This
# order was verified line-by-line against every masking warning R produces
# on install, to guarantee identical collision-resolution behaviour to the
# original, already-tested script. DO NOT reorder these without re-checking
# against that original library() order - reordering silently changes which
# function wins a naming collision (e.g. dplyr::intersect vs terra::intersect),
# which can change plot output with no visible error.

export(run_gfc_dashboard)
export(check_aoi)
export(calc_gfc_tiles)
export(utm_epsg)
export(extract_gfc)
export(threshold_gfc)
export(gfc_stats)
export(update_required_packages)

import(ggfx)
import(shiny)
import(shinydashboard)
import(shinyjs)
import(shinyWidgets)
import(sf)
import(terra)
import(leaflet)
import(leafem)
import(mapgl)
import(DT)
import(ggplot2)
import(patchwork)
import(dplyr)
import(tidyr)
import(tidyterra)
import(scales)
import(animation)
import(jsonlite)
