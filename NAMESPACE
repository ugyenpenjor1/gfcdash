# Generated to match the roxygen2 tags in R/*.R.
# Regenerate authoritatively with devtools::document() once you have
# roxygen2 installed - this hand-built version is a correct starting point
# but devtools::document() should be treated as the source of truth going
# forward, especially if you add/remove any @export or @import tags.
#
# NOTE: every package the original script loaded via library() is imported
# in full here (import(), not importFrom() with a hand-picked function
# list). An earlier version tried to enumerate only the specific functions
# used from mapgl/leaflet/etc., and repeatedly missed some - each miss
# surfaces as "could not find function" only once a user clicks into the
# specific tab that uses it, which is a bad way to discover a typo. Full
# import() exactly matches how these packages behaved when loaded with
# library() in the original script, at the cost of the cosmetic (harmless,
# see utils_pkg.R) "replacing previous import" messages at library() time.

export(run_gfc_dashboard)
export(check_aoi)
export(calc_gfc_tiles)
export(utm_epsg)
export(extract_gfc)
export(threshold_gfc)
export(gfc_stats)
export(update_required_packages)

import(shiny)
import(shinydashboard)
import(shinyjs)
import(shinyWidgets)
import(dplyr)
import(tidyr)
import(ggplot2)
import(patchwork)
import(sf)
import(terra)
import(tidyterra)
import(leaflet)
import(leafem)
import(mapgl)
import(DT)
import(scales)
import(animation)
import(jsonlite)
